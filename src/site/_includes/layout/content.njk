{% extends "layout/base.njk" %}
{% block content %}
  <div class="container">
    <h1 class="title-2">{{title}}</h1>

    <div class="content">
      {{ content | safe }}
    </div>

  </div>
{% endblock %}