<article class="p-8 mb-8 card">

  <h3 class="text-2xl font-bold leading-tight tracking-tight">
    <a href="{{item.link}}" title="{{ item.name }}">{{item.name}}</a>
  </h3>

  {% if item.tags.length%}
    <p class="my-4">
      {% for tag in item.tags %}
        <span class="text-gray-400">#{{tag}}</span>
      {% endfor %}
    </p>
  {% endif %}
  <p class"my-4">
    {{item.description}}
  </p>
  <p class="my-4">
    <a href="{{item.link}}" title="{{ item.name }}" class="btn">Mehr Informationen <span>→</span></a>
  </p>
</article>