---
title: Episoden
eleventyNavigation:
  key: Episoden
  order: 30
nosearch: true
pagination:
  data: episodes.items
  size: 12
  alias: episode
  reverse: false
---
{% extends "layout/base.njk" %}
{% block content %}
  <div class="page-hero">
    <div class="container">
      <h1 class="title-1">{{title}}</h1>
      <div class="page-hero__description">
        <p>
          Regelmäßig veröffentlichen wir hier neue Episoden zu Themen rund um Softwareentwicklung, agile Methoden und
          alles was uns sonst noch so interessiert.
        </p>
      </div>
    </div>
  </div>

  <section class="section">
    <div class="container">

      <div class="grid">
        {% if pagination.items.length %}
          {% for episode in pagination.items %}
            {%- include "episode-card.njk" -%}
          {% endfor %}
        {% endif %}
      </div>

      <nav aria-labelledby="podcast-episodes-pagination" class="section-cta">
        <h2 id="podcast-episodes-pagination" class="sr-only">Podcast Episodes Pagination</h2>
        <div class="pagination-nav">
          {%- for pageEntry in pagination.pages %}
            <a href="{{ pagination.hrefs[ loop.index0 ] }}" 
                           class="btn {% if page.url == pagination.hrefs[ loop.index0 ] %}btn--primary{% else %}btn--outline{% endif %}"
                           {% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="page"{% endif %}>
              {{ loop.index }}
            </a>
          {%- endfor %}
        </div>
      </nav>
    </div>
  </section>
{% endblock %}